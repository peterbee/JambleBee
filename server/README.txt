Requirements:

The host machine must have nodejs and npm installed.

Before running the server:

First, ensure that the user has read/write permissions for all sub folders of the server directory
In the server directory run the command
$ npm install

To start the server, you must start the bin/www program

$ bin/www

To start and send to the background run the start script

$ ./start.sh.
